---
layout: base
title: Tag Archive
pagination:
  data: collections
  size: 1
  alias: tag
  filter: ['all', 'blog', 'primary', 'support']
---

{% set tagListItems = collections[tag] %}

<h1>{{ title }}</h1>

{% include "components/tag-list-items.njk" %}